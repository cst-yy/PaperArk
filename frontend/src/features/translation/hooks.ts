import { useMutation, useQuery, useQueryClient } from "@tanstack/react-query";
import { editTranslationBlock, estimatePageTranslation, getTranslationPage, translatePage } from "./api";

export function useTranslationPage(paperId:string|undefined,documentId:string|undefined,page:number,enabled=true){return useQuery({queryKey:["translation-page",paperId,documentId,page,"zh-CN"],queryFn:()=>getTranslationPage(paperId!,documentId!,page),enabled:Boolean(enabled&&paperId&&documentId)});}
export function useTranslatePage(paperId:string,documentId:string,page:number){const qc=useQueryClient();return useMutation({mutationFn:async()=>{const estimate=await estimatePageTranslation(paperId,documentId,page);const cost=estimate.estimated_cost?`${estimate.currency??""} ${estimate.estimated_cost}`:"无法计算（模型未配置价格）";if(!window.confirm(`翻译第 ${page} 页\n区域：${estimate.total_blocks}\n复用：${estimate.reusable_blocks}\n预计新增 Token：${estimate.estimated_input_tokens+estimate.estimated_output_tokens}\n预计费用：${cost}\n\n使用云端 Provider 时，论文文本会发送至对应服务商。是否继续？`))throw new Error("cancelled");return translatePage(paperId,documentId,page);},onSuccess:()=>qc.invalidateQueries({queryKey:["translation-page",paperId,documentId,page]})});}
export function useEditTranslation(){const qc=useQueryClient();return useMutation({mutationFn:({id,text,revision}:{id:string;text:string|null;revision:number})=>editTranslationBlock(id,text,revision),onSuccess:()=>qc.invalidateQueries({queryKey:["translation-page"]})});}
